import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "A.R.I.A. | Automatic Reporting & Intelligent Analysis",
  description: "Offline-first healthcare AI for real-time medical transcription, entity extraction, ICD-10 coding, and FHIR-compliant documentation.",
  keywords: ["healthcare", "AI", "medical transcription", "FHIR", "ABDM", "ICD-10"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
